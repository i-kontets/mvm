import styles from './Progress.module.css';
export default function Progress({ workouts, metrics }) { const volume = workouts.reduce((sum, item) => sum + Number(item.weight || 0) * Number(item.reps || 0), 0); return <section className={styles.page}><header><p>YOUR PROGRESS</p><h1>進捗</h1><span>登録したデータから、成長を振り返ります。</span></header><div className={styles.metrics}><article><p>WORKOUTS</p><strong>{workouts.length}</strong><span>記録数</span></article><article><p>VOLUME</p><strong>{volume.toLocaleString()}<small> kg</small></strong><span>総トレーニング量</span></article><article><p>LATEST WEIGHT</p><strong>{metrics[0] ? metrics[0].weight : '—'}{metrics[0] && <small> kg</small>}</strong><span>{metrics[0] ? metrics[0].date : '未記録'}</span></article></div>{workouts.length === 0 && metrics.length === 0 ? <div className={styles.empty}><b>振り返るデータがまだありません</b><p>ワークアウトや体重を登録すると、ここに進捗が表示されます。</p></div> : <section className={styles.note}><p>次のステップ</p><b>記録が増えると、体重・トレーニング量・種目ごとの伸びをグラフで表示できます。</b></section>}</section>; }
