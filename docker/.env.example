# 必要な場合だけ、これを docker/.env にコピーして値を変更してください。
# Docker Composeは値を未指定の場合でも開発用の既定値で起動します。
MYSQL_DATABASE=mvm
MYSQL_USER=mvm_user
MYSQL_PASSWORD=mvm_password
MYSQL_ROOT_PASSWORD=mvm_root_password
MYSQL_PORT=3306
PMA_PORT=8080
